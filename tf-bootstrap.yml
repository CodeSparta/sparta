---
- name: ocp4-deploy | Deploy bootstrap node with terraform
  hosts: localhost
  connection: local
  roles: 
    - tf-deploy-bootstrap

- name: ocp4-deploy | Wait for bootstrap node to come up
  hosts: localhost
  connection: local
  tasks:
    - name: ocp4-deploy | task03-deploy | Wait for registry to come up
      pause:
        minutes: 2